# Roadmap

Phased per the governing specification. Each phase: implement -> test -> inspect -> fix -> document -> verify, then continue. Phase 0 is complete with this foundation.

| Phase | Scope | Exit criteria (verified) |
| --- | --- | --- |
| **0 — Foundation** (done) | Research, threat model, capability matrix, architecture, PRD/DRD, security/privacy docs, ADRs, this roadmap | All required documents exist and are internally consistent; capability claims verified against cited sources |
| **1 — Project foundation** (done) | Gradle multi-module scaffold (/core KMP + /android), CI skeleton (lint, detekt, gitleaks, unit tests), minimal Compose shell app with disclosure-first onboarding | Verified 2026-09-21: `detekt` clean, `:core:domain:jvmTest` 10/10, `lintDebug` clean, `assembleDebug` produces APK; manifest declares **zero** permissions (INTERNET deferred to Phase 3 per least privilege) |
| **2 — Security core** (done) | Event model + event bus + encrypted Room event store + risk engine skeleton + policy engine + retention enforcement | Verified 2026-09-21: 35/35 JVM tests (schema round-trip, encryption/tamper/IV-uniqueness, risk determinism, retention deletion); detekt + Android lint clean; APK assembles; Room schema v1 exported. Keystore keyring implemented (StrongBox-aware); **on-device execution pending** (sandbox has no emulator/KVM) — first device run must re-verify |
| **3 — Link Guardian** (done) | URL pipeline (normalization, canonicalization, punycode/IDN, heuristic indicators, brand impersonation heuristics), local feed lookup, share-intent entry, manual check UI, verdict + evidence screens | Verified 2026-09-25: 52/52 JVM tests incl. labeled corpus (10 phishing-pattern, 6 benign), 2000-case parser fuzz + 1000-case verdict fuzz (zero crashes); offline = UNKNOWN (tested); heuristics capped at WARNING per policy (tested); share-target + manual check UI built; evidence-first verdict screen; events recorded to encrypted store. **Honest caveats:** corpus-scale accuracy targets (SECURITY_TESTING §8 ≥97% precision) need larger labeled corpora — tracked to Phase 11; on-device execution still pending (no emulator in sandbox) |
| **4 — Network Guardian** (done) | VpnService local loopback DNS filter, per-UID attribution where feasible, blocklist policy, connection timeline, prominent disclosure + consent flow, Play declaration prepared | Verified 2026-09-26: split-tunnel design (only DNS routed in; all other traffic bypasses the app entirely — no remote endpoint exists); blocked names answered REFUSED purely on-device (zero egress); allowed lookups forwarded unmodified to the system resolver; 70/70 JVM tests incl. hardened DNS parser fuzz (3000 packets, only DnsParseException), IP/UDP codec with independently verified checksum golden value (0xA9E2), filter tier policy + determinism; prominent-disclosure consent flow + foreground notification + honest OFF/establish-failure states; block events recorded to encrypted store. **Honest caveats:** on-device tunnel verification pending (no emulator in sandbox); IPv6 DNS bypass not yet detected/handled; single-threaded pump (sequential query handling); battery benchmark pending (SECURITY_TESTING §7); per-UID attribution deferred to Phase 5; sample feed only (real blocklists with TI integration) |
| **5 — App Guardian** (done) | Package inventory (visibility policy), permission analysis, installer source, signature info, risk signals; QUERY_ALL_PACKAGES declaration | Verified 2026-09-26: deterministic permission catalog (~60 classified + UNCLASSIFIED-never-ignored policy), capability-combination detection (stalkerware/banking-trojan combos incl. OTP interception), installer-provenance and legacy-target-SDK findings; labeled corpus tests (benign LOW, stalkerware CRITICAL with named combos); grant state never read/claimed (tested reason present); data-minimizing events (summary counts only; per-app events capped at 8, HIGH/CRITICAL only); QUERY_ALL_PACKAGES declared with Play-declaration TODO, lint downgraded to warning with justification (not disabled). **Honest caveats:** inventory correctness on real devices across API 26-35 pending (no emulator in sandbox); signature info deferred (see ADR candidates); label for system-app hidden capabilities documented |
| **6 — Privacy Engine** (done) | Observable privacy facts: install/update/remove summaries, permission-change snapshots, timestamp-only boot records, patch age + verified-boot facts, privacy coverage matrix with honest NOT MONITORED/OS LIMITATION labels; Play Integrity deferred to Phase 12 (no verdict dependency added) | Verified 2026-09-26: 99/99 JVM tests (snapshot diff incl. caps, boot-window facts, coverage honesty, patch-date math incl. leap day); detekt + Android lint clean; APK assembles; RECEIVE_BOOT_COMPLETED + package/boot receivers; best-effort recording (render-first). **Honest caveats:** pre-install history unknowable (no backfill); dedupe is process-lifetime (restart may redeliver one fact); permission snapshots cover the main profile only (Private Space invisible); verified-boot unreadable on some OEM builds (UNKNOWN, stated); boot count reads our own recorded events; on-device run pending (no emulator in sandbox) |
| **7 — Identity Exposure** (done) | Encrypted identity vault (Keystore-wrapped AES-256-GCM, private file), HIBP v3 integration behind explicit per-identity consent (range mode default, full-address mode needs separate tick), exposure states | Verified 2026-09-26: 109/109 JVM tests (pure-Kotlin SHA-1 vs standard vectors, normalization/masking, 6-char prefix match, breach-response minimization, consent text, never-safe language); detekt + Android lint clean; APK assembles. k-anonymity preferred (range mode only sends 6 hash chars; matching on-device); full-address mode sends URL-encoded address with explicit consent; stored breach facts minimized to names/dates/data-classes; masked refs only in events/UI; addresses retained until user deletes (retention null per policy); API key session-only; HIBP-only egress allowlist enforced. **Honest caveats:** HIBP subscription key required (user-supplied); no-match never means safe (HIBP index incomplete — stated in UI); biometric gate deferred (vault is encrypted, Keystore-wrapped, but not biometric-gated — tracked to hardening); rate limits surface as CHECK_FAILED with reason; on-device run pending (no emulator in sandbox) |
| **8 — Incident Correlation** (done) | Correlation engine (30-min time-window entity grouping), incident model, timeline UI with "occurred shortly after" language | Verified 2026-09-26: 120/120 JVM tests (synthetic event streams: window splits, entity isolation, order-independence, severity sorting; causal-lint rejects causal verbs); causal-claim lint on all rendered copy (CausalLanguageTest mirrors every UI string); detekt + Android lint clean; APK assembles. Singles never merged, listed separately. **Honest caveats:** correlation is temporal proximity only — never causal (stated in UI); 30-min window is fixed, not adaptive; reads the encrypted store on demand (no background correlation); on-device run pending (no emulator in sandbox) |
| **9 — UI/UX** (done) | Full information architecture (HOME/SECURITY/NETWORK/APPS/IDENTITY/TIMELINE/SETTINGS), restrained visual system, accessibility pass | Verified 2026-09-26: 129/129 JVM tests (7-section order/routes/descriptions; WCAG 2.1 AA on all rendered text pairs — teal measured large-text-only and removed from body text; 48dp floor; words-not-color posture labels; no auto-animation); disclosure-first onboarding kept, dismissal to tabs with rotation-safe state; deletion receipt in Settings; detekt + Android lint clean; APK assembles. **Honest caveats:** screen-reader and on-device run pending (no emulator in sandbox); reduced-motion honored by shipping no auto-playing animation; no Play release (debug-signed) |
| **10 — Security hardening** (done) | Threat-model-driven review, pen test, flag review (exported components, FLAG_SECURE), dependency audit, R8/obfuscation review | Verified 2026-09-26: 13 findings fixed (H1–H13 in docs/security/HARDENING_REPORT.md: IHL/pump fail-open holes, share-input bounds, HIBP case-miss + body bounds, vault codec validation, masked-render crash, block-record truncation, key masking, tap-jacking guard, FLAG_SECURE, cleartext ban, resource shrinking); 138/138 JVM tests (129 baseline + 9 new); detekt + Android lint clean; APK assembles. **Honest caveats:** no external pen test; no on-device run and no release-APK smoke (no emulator in sandbox); IPv6 DNS bypass, single-threaded pump, battery benchmark, and corpus-scale accuracy carry to Phase 11; biometric gate to hardening follow-up, Play Integrity to Phase 12; no dependency vulnerability scan or lockfiles in CI (gitleaks only) |
| **11 — MVP testing** | Full SECURITY_TESTING.md matrix: unit/integration/instrumentation/security, battery/memory benchmarks, false-positive field study | All gates pass; performance numbers recorded, not assumed |
| **12 — Release preparation** | Store listing with honest capability statements, privacy policy, Play declarations, staged rollout plan, incident-response dry run | Release checklist green |

## V2 (post-MVP)

Secure browsing environment (in-app WebView with injected client-side checks), remote sandbox APK analysis (ADR-gated, isolated), deeper identity graph, richer TI integrations, desktop investigation begins.

## V3

Desktop agent (Windows first), cross-device dashboard reusing `/core`.

## Non-goals (all versions)

TLS interception; accessibility-based content inspection; notification body scanning without a full privacy re-review; marketing-driven fake signals.
